apiVersion: v1
kind: Pod
metadata:
  name: web
  namespace: default
spec:
  containers:
    - name: web
      image: nginx:lastest
      ports:
        - name: web
          protocol: TCP
          containerPort: 80

